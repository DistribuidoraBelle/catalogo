Sin confirmar 968051.crdownload

# ── App Android ──────────────────────────────────────────────
# La llave de firma NUNCA va al repositorio (es publico).
# Vive en C:\Belle\app-firma\ y en GitHub como secreto.
*.p12
*.jks
*.keystore
app-android/build/
app-android/app/build/
app-android/.gradle/
app-android/local.properties
